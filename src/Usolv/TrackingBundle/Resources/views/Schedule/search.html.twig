{% extends 'UsolvTrackingBundle:Template:layout.html.twig' %}

{% block title %}Schedule{% endblock %}

{% block head_script %}
<script src="{{ asset('bundles/usolvtracking/js/schedule-search.js') }}" type="text/javascript"></script>
{% endblock %}
  
{% block content %}

{#
    <!-- Begin page content -->
    <div class="container">
      <form class="form-horizontal" role="form">
        <div class="form-group">
          <label class="control-label col-sm-2" for="project">Project:</label>
          <div class="col-sm-10">
            <div class="col-xs-4">
              <select class="form-control" id="project">
                <option value='0'>All projects</option>
                <option value='1'>Project 1</option>
                <option value='2'>Project 2</option>
                <option value='3'>Project 3</option>
              </select>
            </div>
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <div class="col-xs-4">
              <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-search"></span> Search</button>
            </div>
          </div>
        </div>
      </form>
    </div>
#}

    {{ form_start(form, {'attr': {'class':'form-horizontal', 'role':'form'}}) }}
    <div class="form-group">
          <label class="control-label col-sm-2" for="project">Project:</label>
          <div class="col-sm-10">
            <div class="col-xs-6">
              {{ form_widget(form.project, {'attr': {'class': 'form-control'}}) }}
            </div>
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <div class="col-xs-6">
              <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-search"></span> Search</button>
            </div>
          </div>
        </div>
    
    <table id="list2"></table>
    <div id="pager2"></div>
    <div id="output"></div>
    
    {# { form_end(form) } #}
  
    {#
    {% for item in tasks %}
    {{ item.moduleId }} " - " {{ item.wbsid }} ": " {{ item.planstart|date('Y-m-d') }} " => " {{ item.planfinish|date('Y-m-d') }} " (" {{ item.plancost }} ")" <br/>
    {% endfor %}
    #}

{% endblock %}
